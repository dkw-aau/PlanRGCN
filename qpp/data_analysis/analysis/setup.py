from setuptools import setup, find_packages

setup(
    name='analysis',
    version='1',
    description='A helper module for analysing a SPARQL query workload',
    author='Abiram',
    author_email='abiramm@cs.aau.dk',
    packages=find_packages(),
    install_requires=[],
)